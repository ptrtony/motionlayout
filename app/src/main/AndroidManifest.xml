<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.motionlayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Motionlayout">
        <activity android:name=".motionlayoutDemo.Android11EditActivity"></activity>
        <activity android:name=".motionlayoutDemo.MotionLayoutDemoActivity" />
        <activity android:name=".motionlayoutDemo.MainActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".music.ImageFaceActivity"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Main1Activity" />
    </application>

</manifest>